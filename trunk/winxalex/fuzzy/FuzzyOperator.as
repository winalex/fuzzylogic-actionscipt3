package winxalex.fuzzy 
{
	/**
	 * ...
	 * @author alex winx
	 */
	public class static FuzzyOperator
	{
		
		
		
		public  static function AND(...args):FuzzyTerm
		{
			var faz:Fazzificatior;
			for each (var func:FuzzyManifold in  faz)
				  {
					  func[args
				  }
		}
		
		public static function OR(...args):FuzzyTerm
		{
/*			double FzOR::GetDOM()const
{
  double largest = MinFloat;

  std::vector<FuzzyTerm*>::const_iterator curTerm;
  for (curTerm = m_Terms.begin(); curTerm != m_Terms.end(); ++curTerm)
  {
    if ((*curTerm)->GetDOM() > largest)
    {
      largest = (*curTerm)->GetDOM();
    }
  }

  return largest;
}*/
		}
		
	}

}